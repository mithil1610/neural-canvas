platform :ios, '15.0'

# CocoaPods analytics sends network stats synchronously affecting flutter build latency.
ENV['COCOAPODS_DISABLE_STATS'] = 'true'

project 'Runner', {
  'Debug' => :debug,
  'Profile' => :release,
  'Release' => :release,
}

def flutter_root
  generated_xcode_build_settings_path = File.expand_path(File.join('..', 'Flutter', 'Generated.xcconfig'), __FILE__)
  unless File.exist?(generated_xcode_build_settings_path)
    raise "#{generated_xcode_build_settings_path} must exist. If you're running pod install manually, make sure flutter pub get is executed first"
  end

  File.foreach(generated_xcode_build_settings_path) do |line|
    matches = line.match(/FLUTTER_ROOT\=(.*)/)
    return matches[1].strip if matches
  end
  raise "FLUTTER_ROOT not found in #{generated_xcode_build_settings_path}. Try deleting Generated.xcconfig, then run flutter pub get"
end

require File.expand_path(File.join('packages', 'flutter_tools', 'bin', 'podhelper'), flutter_root)

flutter_ios_podfile_setup

target 'Runner' do
  use_frameworks!
  use_modular_headers!

  flutter_install_all_ios_pods File.dirname(File.realpath(__FILE__))
  
  target 'RunnerTests' do
    inherit! :search_paths
  end
  
  # Properly nested share extension block to cleanly inherit the main engine environments natively
  target 'ShareExtension' do
    inherit! :search_paths
  end
end

post_install do |installer|
  # 1. Parse the true engine framework path dynamically from Flutter's internal manifest
  flutter_framework_dir = nil
  generated_xcconfig = File.expand_path(File.join('..', 'Flutter', 'Generated.xcconfig'), __FILE__)
  if File.exist?(generated_xcconfig)
    File.foreach(generated_xcconfig) do |line|
      matches = line.match(/FLUTTER_FRAMEWORK_DIR\=(.*)/)
      flutter_framework_dir = matches[1].strip if matches
    end
  end

  installer.pods_project.targets.each do |target|
    flutter_additional_ios_build_settings(target)
    target.build_configurations.each do |config|
      config.build_settings['IPHONEOS_DEPLOYMENT_TARGET'] = '15.0'
      # Permits application extensions to compile shared background delegate hooks safely
      config.build_settings['APPLICATION_EXTENSION_API_ONLY'] = 'NO'
      
      # 2. AUTOMATED DRAG-AND-DROP EMBED LINKAGE:
      # Inject the direct binary pathways into the Clang environment scanner
      if flutter_framework_dir
        config.build_settings['FRAMEWORK_SEARCH_PATHS'] ||= ['$(inherited)']
        config.build_settings['FRAMEWORK_SEARCH_PATHS'] << "\"#{flutter_framework_dir}\""
        config.build_settings['FRAMEWORK_SEARCH_PATHS'] << '"$(SRCROOT)/../Flutter"'
        
        config.build_settings['HEADER_SEARCH_PATHS'] ||= ['$(inherited)']
        config.build_settings['HEADER_SEARCH_PATHS'] << "\"#{flutter_framework_dir}/Flutter.xcframework/ios-arm64/Flutter.framework/Headers\""
        config.build_settings['HEADER_SEARCH_PATHS'] << '"$(SRCROOT)/../Flutter"'
      end
    end
  end
end